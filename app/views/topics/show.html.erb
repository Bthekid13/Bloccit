<h1><%= @topic.name %></h1>
<div class="row">
  <%= render @topic.posts %>
  <div class="col-md-4">
    <% if user_can_edit_topics? %>
      <%= link_to "Edit Topic", edit_topic_path, class: 'btn btn-success' %>
    <% end %>
    <% if user_can_destroy?(@post) %>
      <%= link_to "Delete Topic", @topic, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure that you want to delete this topic?'} %>
    <% end %>
      <%= link_to "New Post", new_topic_post_path(@topic), class: 'btn btn-success' %>
    <% if user_can_destroy?(@post) %>
      <%= link_to "Delete Post", [@topic], method: :delete, class: 'btn btn-danger', data: { confirm: "Are you sure that you want to delete this topic?"} %>
    <% end %>
  </div>
</div>
